import { ComponentTreeInfo } from './component-tree-info';

describe('ComponentTreeInfo', () => {
  it('should create an instance', () => {
    expect(new ComponentTreeInfo()).toBeTruthy();
  });
});
